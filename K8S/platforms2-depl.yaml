apiVersion: apps/v1
kind: Deployment
metadata:
  name: platforms2-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: platformservice
  template:
    metadata:
      labels:
        app: platformservice
    spec:
      containers:
        - name: platformservice
          image: nechaevaanastas/platformservice2:latest
---
apiVersion: v1
kind: Service
metadata:
  name: platforms-clusterip-srv
spec:
  type: ClusterIP
  selector:
    app: platformservice2
  ports:
  - name: platformservice2
    protocol: TCP
    port: 80
    targetPort: 80
